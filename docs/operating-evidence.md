# 운영 증빙과 외부 감시

2026-09-07 구현. 주문 조건을 완화하거나 실계좌를 해제하지 않는다. 신규 라이브러리·DB 없이 기존 계좌별 파일과 주문 작업 큐를 재사용한다.

## 구현과 실제 검증을 구분한다

| 항목 | 구현 | 실제 확인 / 남은 조건 |
| --- | --- | --- |
| 잔고·체결 대조 | 과거 주문번호·종목·날짜·주문시각·방향·수량으로만 수정, 수정 전후 증빙 보존 | 키움 모의 SE 누락 매도 51주 × $119.74 복구 완료, 잔고 차이 0. 한국 날짜뿐 아니라 미국 거래일도 조회하도록 수정 |
| 알람 확인 | 공유 목록과 활성 증빙 분리, 확인일·만료일·최종 수신 별도 저장 | TradingView 화면에서 50종목의 4시간·일봉 100개 Active 확인. 만료일·개별 웹훅 URL은 이번 목록 확인만으로 보장하지 않음 |
| 외부 감시 | GitHub에서 /health 점검, 장애·복구만 Discord 알림 | Mac과 독립. 5분 예약은 실시간 SLA가 아니며 실행 지연·누락 가능 |
| 비용·총자산 | 공식 정산 API, 명세서 비용 입력, 일별 자산 표본과 입출금 조정 계산 | 한투 모의 총자산 응답 확인. 모의 정산 API 미지원분은 명세서 필요. 과거 비용·입출금을 0으로 만들지 않음 |
| 정책 고정·후보 관찰 | 실계좌 시작 시 코드·설정 지문 승인 요구, 향후 신호의 Sigma 후보 관찰 분리 | 기존 정책 유지. 미래 신호 표본이 쌓여야 비교 가능. 전체 전략 백테스트나 실계좌 성과 검증이 아님 |
| Mac 중단 시 손절 | 키움 미국주식 STOP 등록·조회·취소·보호수량 조정 구현. 불명 응답은 재주문 차단 | 자동검증 통과와 실제 계좌 지원 검증은 별개. 현재 비활성. 한투 해외 REST에는 검증된 STOP 경로 없음 |

## 계좌 증빙

실행기 시작 시 및 하루 간격으로 증권사 과거 체결·정산·자산을 조회한다. 실행기 소유자는 시스템/주문승인 채널에서 `!account reconcile`로 다시 대조할 수 있다. 이 명령은 주문·취소를 보내지 않는다.

- 수량이 모자란다는 이유만으로 매도를 만들어 넣지 않는다. 수동 매매·권리변동도 실제 증빙을 확인해야 한다.
- 연속 조회의 중간 페이지 실패, 날짜·주문번호 중복, 정정주문 체인, 미종결 잔량은 자동 수정하지 않는다.
- 키움 과거 주문은 한국 날짜와 미국 거래일을 모두 조회하고, 기록의 한국 주문시각까지 2분 이내인지 대조한다. 날짜마다 재사용될 수 있는 주문번호만으로 연결하지 않는다. 정확한 체결시각이 없으면 날짜를 조합해 만들지 않는다.
- 실제 체결 증빙은 `<수신기록파일>.evidence.json`에 먼저 저장하고 주문 기록을 변경한다. 기록 수정 중 다른 갱신이 발생하면 다시 대조한다.
- 정산행은 종목·방향·일자·총체결수량·평단이 단일 주문과 일치할 때만 비용을 연결한다. 분할체결·일별 합산으로 구별되지 않으면 명세서가 필요하다.
- 한투 정산은 수수료와 실제 매매금액/정산금액 차이가 확인될 때 전체 비용으로 기록한다. 이미 체결가에 포함된 가격 차이를 손익에서 재차 차감하지 않는다.
- 총자산은 주문가능금액과 다르다. 자산곡선은 공식 평가 조회만 사용한다. 한투 대출 포함 계좌는 순자산 명세서 검증 전 제외한다.

### 모의 API 미지원 시 명세서 반영

`docs/account-statement.example.json`을 참고해 `<수신기록파일>.KIWOOM.statement.json` 또는 `.KIS.statement.json`을 로컬에 만든다. 현재 계좌·환경과 일치해야 하며, 증권사 화면/명세서에서 확인한 사실만 입력한다. 파일명 패턴은 Git에서 제외된다. 계좌번호·API 키는 넣지 않는다.

소유자가 `!account import`를 실행해야 반영한다. 파일을 만드는 것만으로 적용하지 않는다. 과거 실제 체결시각을 모르면 `filledAt: null`로 두며, 복구 시각을 체결시각으로 만들지 않는다. 명세서 `executionCosts`는 비용 전체가 확인될 때만 넣는다. `executions: []`로 입출금 전체 내역만 반영할 수도 있다.

입출금은 입금 양수/출금 음수이며 외부 입출금만 적는다. 매매대금·배당·수수료를 입출금으로 빼면 수익률이 왜곡된다. 외화 범위에서는 환전 유입/유출도 해당 범위의 외부 흐름으로 취급한다. 시작·종료시각 사이의 **전체** 내역을 확인한 후에만 `cashFlowCoverage`를 넣는다. 무입출금 기간도 확인한 명세서가 있어야 한다.

`!account performance`의 계좌 자산 성과는 미실현 평가를 포함한 일별 표본의 연결 Modified Dietz 수익률/낙폭이다. 표본 2개와 그 사이 입출금 전체 증빙이 필요하다. 장중 MDD·전기간 최고 낙폭이라고 부르지 않는다. 국내/해외·통화·계좌/모의실전 범위를 합산하지 않는다.

## 알람 증빙

- `!alerts status`: 각 종목·시간봉의 확인일, 만료일, 최종 수신 기록 내려받기.
- `!alerts verify NASDAQ:NVDA 240 알람ID 2026-10-01`: 소유자가 화면에서 확인한 활성 알람과 만료일 등록. 무기한일 때만 `never`.
- 확인 후 7일이 지나면 재확인 대상으로 표시한다. 이는 Discord 표시 정책이며 TradingView의 실제 만료일을 바꾸지 않는다.
- `.runtime/tradingview-alert-verification.json`: 브라우저 화면에서 읽은 활성 목록의 로컬 증빙. 종목 거래소가 유일하게 연결될 때만 적용한다. 화면상 Active 확인은 웹훅 전달 성공을 뜻하지 않는다.
- 새 수신은 별도 마지막 수신시각을 갱신한다. 무신호가 장애라는 추측으로 알람을 재생성하지 않는다.
- TradingView 알람은 생성 당시 스냅샷이므로 지표/입력 변경이 기존 알람에 자동 반영되지 않는다. [공식 알람 문서](https://www.tradingview.com/pine-script-docs/faq/alerts/#if-i-change-my-script-does-my-alert-change)

## 외부 감시 연결

`.github/workflows/external-health.yml`은 매시 2분부터 5분 간격으로 GitHub 실행기에서 점검한다. 주문 경로가 아닌 `/health`만 호출하며, 계좌/토큰/잔고를 로그나 공개 이슈에 넣지 않는다. 신호 서버의 Discord 연결과 등록된 계좌 실행기 상태도 점검 대상이다. 신호 서버가 계좌중립 모드인데 계좌 실행기 상태 파일이 하나도 없어도 비정상 처리한다.

GitHub 비밀값은 `MONITOR_HEALTH_URL`, `MONITOR_DISCORD_WEBHOOK` 두 개뿐이다. 시스템상태 채널의 알림 전용 웹훅이며 전체 봇 토큰과 증권사 키를 GitHub에 저장하지 않는다. 저장소 `issues:write`는 공개 이슈에 일반적인 장애/복구 상태만 기록하기 위해 사용한다. 반복 장애 시 이슈 하나를 유지하고 알림을 반복하지 않는다. 알림 응답이 유실되면 다음 실행에서 재전송될 수 있다.

로컬 봇에 해당 채널 웹훅 관리 권한이 이미 있다면 `node --env-file=.env.signal scripts/setup-external-monitor.cjs <채널ID> <소유자/저장소>`로 설정할 수 있다. 권한이 없다면 서버 소유자의 Discord 화면에서 전용 웹훅을 생성하고 GitHub Actions secret으로 넣는다. 봇의 서버 전체 권한을 넓힐 필요는 없다. `notify_test=true` 수동 실행은 실제 장애가 아니라는 문구로 알림 전송만 검증한다.

예약은 [GitHub 공식 문서](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule)상 지연/누락될 수 있고, 공개 저장소에 60일간 활동이 없으면 비활성화될 수 있다. 정확한 실시간 SLA가 필요해지면 독립 감시 서비스를 추가해야 한다. **이 기능은 중단 통지이지 손절 주문이 아니다.**

## 정책 고정과 주문 없는 후보 관찰

실계좌 읽기 전용 점검은 그대로 가능하다. 실제 주문이 가능한 실행기 시작에는 기존 실계좌 잠금 해제뿐 아니라 `ACCOUNT_APPROVED_POLICY_HASH`와 현재 정책 코드/위험설정 지문 일치도 필요하다. 변경된 지문을 자동으로 승인하지 않는다. 기존 모의계좌 설정은 바꾸지 않는다.

`forward-policy-study.json`은 배포 후 새 신호만 기록한다. 과거 웹훅 복구/재전송은 새 실험으로 사용하지 않는다. 현재 비교는 실계좌 Sigma 2.5 상한과 모의계좌 3.5 상한의 **진입 필터 관찰 연구**다. 다른 조건은 같은 신호에서 고정하고, 다음 동일 종목·시간봉 신호까지 가격 변화를 기록한다. 현금·수량·체결·부분매매를 시뮬레이션하지 않으므로 전체 전략 기대수익/승률로 해석해서는 안 된다. 거래가 발생한 실제 성과와 별도 자료로 제공한다.

## 증권사 손절 보호

키움 미국주식 REST 매도 API의 `trde_tp=35 STOP`, `34 STOP LIMIT`, `stop_pric`는 [공식 ust20001 명세](https://openapi.kiwoom.com/m/guide/apiguide/38/ust20001)에 있다. 그러나 요청 필드 존재만으로 해당 계좌/세션의 지원, 주문 유효기간, Mac 중단 후 유지·발동, 부분매도 후 보호수량 갱신을 검증했다고 볼 수 없다.

구현된 동작:

- 기존 계좌 작업 큐·주문 기록·수신 기록을 재사용한다. 송신 의도를 먼저 디스크에 남겨 응답유실·재시작 후 중복 STOP 등록을 막는다.
- 자동매매 관리분의 실제 체결 수량만 보호한다. 일부만 매수 체결되어도 그 부분을 보호하며, 수동 보유분이나 미체결 매수 잔량은 포함하지 않는다. 보유수량·매도가능수량·현재가와 저장된 손절가를 확인한다.
- 주문번호·주문시각·종목·매도 방향·유형 `35`·수량·발동가를 증권사 조회로 검증한다. 누락된 수량을 0으로 처리하지 않는다. 시세가 이미 손절가 이하면 즉시 체결될 STOP으로 대체하지 않고 알린다.
- 일반 매도/추가매수 전에 기존 STOP의 취소 **완료**를 확인한다. 취소 도중 체결된 수량은 먼저 장부에 반영하고 남은 수량으로 다시 계산한다. 취소 접수·조회 실패·응답유실이면 기존 재확인 큐에 보류한다.
- 잔여 보유수량 또는 손절가가 바뀌면 취소 확인 후 다음 점검에서 재등록한다. 증권사에서 지난 주문의 종료를 확인하지 못하면 날짜가 바뀌어도 임의 만료·재등록하지 않는다.
- `!account protection` / `!계좌 보호`로 활성 여부와 개별 STOP 상태를 조회한다. 접수·체결 결과는 기존 체결 로그·매매 기록에 표시한다.

실계좌에서만 `ACCOUNT_BROKER_PROTECTION=true`를 명시하면 자동 연계 후보가 된다. 계좌 잠금과 승인된 정책 지문 조건은 그대로 필요하다. **현재 설정은 변경하지 않았고, 모의 자동매매도 기존대로 유지한다.** STOP 미활성/미지원인 실계좌 신규 매수는 차단하며 기존 보유분 청산은 막지 않는다. 한투 [공식 해외 주문 예제](https://github.com/koreainvestment/open-trading-api/blob/main/examples_llm/overseas_stock/order/order.py)에 있는 미국 매도 주문유형은 지정가와 장 시작/종료 주문이며 STOP은 없다. 국내 HTS 기능을 해외 REST 주문으로 오인해 연결하지 않는다.

아직 실제 계좌에서 확인할 사항: 별도 승인한 모의 소량 STOP 접수·유형/가격/수량 조회·즉시 취소, 실제 계좌별 지원/유효기간과 Mac 중단 중 유지·발동 여부. 이후 실계좌 주문 검증은 별도 승인이 필요하다. 현재 활성 STOP이 있다고 주장하지 않는다. 이 연계는 매수와 STOP을 동시에 체결시키는 원자적 브래킷 주문이 아니며, 매수 후 확인·등록 사이와 수량 재조정 중에는 보호 공백이 있다. 정기 점검 간격은 30초이고 네트워크 지연이 더해질 수 있다. STOP 시장가는 갭·급변 시 체결가격을 보장하지 않으며 GTC/다음 거래일까지 유지된다고 가정하지 않는다.

## 2026-09-07 보안 보강

- 비인증 요청의 URL 해석 실패는 `400 invalid_url`로 종료합니다. 기존 웹훅 서버를 사용한 격리 회귀 테스트에서 잘못된 요청을 거부한 뒤 `/health`가 계속 정상 응답하는지 검사합니다. 운영 서버를 대상으로 장애 입력을 보내지 않습니다.
- 참고자료 이미지의 실제 경로(`realpath`)가 `RESEARCH_DIR` 안에 있는 일반 이미지 파일인지 확인합니다. 절대·상대 경로와 심볼릭 링크를 통한 폴더 이탈, 디렉터리, 없는 파일은 첨부하지 않습니다. Telegram 원문을 수집한 Markdown을 사용하는 격리 테스트로 검증합니다.
- Discord 계좌 명령과 BUY 승인에 소유자·서버·실제 채널 확인을 공통 적용했습니다. 같은 이름의 타 서버 채널, DM, 다른 사용자/봇은 거부하며 이름이 중복된 기록 채널은 임의로 선택하지 않습니다. 정상 승인과 자동매매 ON/OFF는 기존 동작을 유지합니다.
- Node 24.20.0 LTS(Apple Silicon)를 공식 배포본과 체크섬 확인 후 설치했습니다. `.nvmrc`와 시작 스크립트로 프로젝트 버전을 고정했고 다른 프로젝트의 기본 Node는 변경하지 않았습니다. [Node 공식 지원 현황](https://nodejs.org/en/about/previous-releases).
- 로컬 `.env.account`, `.env.signal`, `.env.backup`, 사용자 작성용 env를 `600`으로 제한했습니다. 시작 스크립트도 읽을 설정 파일의 권한과 새 파일 기본 권한을 제한합니다. 파일 내용·키·모의/실계좌 선택은 변경하지 않았습니다.
- 현재 계좌의 주문승인·체결로그·매매일지·포트폴리오와 공용 시스템상태 채널에서 `@everyone` 열람을 차단했습니다. 사용자가 허용한 추가 열람자는 유지하고, 실행 봇의 열람·메시지 전송·이력 조회 권한을 다시 확인했습니다. 시스템상태는 기존 봇 5개의 접근을 유지하며, 봇에 관리자 권한을 부여하지 않았습니다. 열람 허용은 주문 승인 권한 부여가 아닙니다.

이 보강은 위에서 확인한 결함과 설정을 대상으로 합니다. 알려지지 않은 취약점이나 기기 침해가 없음을 보증하는 정밀 보안 인증은 아니며 실계좌 주문 잠금·모의 자동매매 정책은 유지합니다.

Node 24.20.0에서 새 보안 회귀 테스트를 포함한 `npm run self-test` 전체 통과, `npm audit` 알려진 취약점 0건을 확인했습니다. 명령 권한 테스트에는 정상 채널명→ID 확인, 동명 채널 중복 거부, 정상 ON/OFF·승인과 잘못된 서버/사용자/채널 거부가 포함됩니다. 기존 iTerm 서버 탭 두 개에서 재시작했고 새 창·탭이나 숨은 서비스를 만들지 않았습니다. ngrok 프로세스는 유지했으며 로컬 `/health` 정상, 모의 자동매매 ON·실계좌 잠금 유지, 재시작 전 상태 백업을 확인했습니다.

## 검증

`npm run self-test`에 증빙의 정확/모호 연결·미국 거래일 대조, 오래된 기록 덮어쓰기 차단, 비용 미확인, 연속조회 실패, 입출금 조정, 알람 증빙 만료, 실계좌 정책 지문, 재생 신호 제외, 외부 장애/복구·중복 알림 방지 검증이 포함된다. 보호주문은 STOP 요청 형식, 중복/응답유실, 불완전한 조회, 취소 중 부분체결, 부분매수 보호, 일반 매도 전 취소 확인, 실계좌 미지원 매수 차단을 검사한다. 이 자동검증은 테스트 데이터이며 실제 브로커 주문을 보내지 않는다.

2026-09-07 실제 외부 연결 확인:

- GitHub 예약 워크플로 `External service health` 활성화 확인.
- [알림 전달 테스트](https://github.com/Felix0708/stock-trading/actions/runs/34095921349) 성공, 시스템상태 채널에서 `Stock-Trading external health`의 테스트 메시지 도착 확인.
- [실제 외부 상태 점검](https://github.com/Felix0708/stock-trading/actions/runs/34095963247) 성공, `External health: OK` 확인. 정상 상태에서는 추가 알림을 보내지 않음.
- 실제 서버를 끄는 장애 실험은 하지 않았다. 장애·복구·중복 억제 분기는 자동검증으로 확인했으며, 예약 실행의 시간 정확도를 보장하지 않는다.
- 기존 iTerm 두 서버 탭을 재사용해 재시작했고, 신호 서버 `/health` 정상, 활성 알람 증빙 100개 반영, 한투 첫 총자산 표본 저장을 확인했다. ngrok 탭·프로세스는 유지했다.
- 초기 키움 증빙 조회에서 빈 종목코드가 `1517` 오류를 일으켰다. [공식 과거조회 문서](https://openapi.kiwoom.com/m/guide/apiguide/32/ust21150)에는 생략 시 전체라고 되어 있지만 실제 모의 응답과 차이가 있어, 대조 대상 종목·거래소를 명시하도록 보완했다.
- 이후 SE의 한국 날짜 2026-08-18 조회 0건 원인을 추가 확인했다. 미국 거래일 2026-08-17 조회에서 기존 주문과 주문시각까지 일치하는 매도 51주 × $119.74, 잔량 0, 체결완료 원본을 찾았다. 정산내역·자산 조회의 `RC9000` 미지원과 체결 이력은 별개다. 정확한 체결시각과 비용은 제공되지 않아 추정하지 않는다.
- 기존 실행기를 중단하고 주문·수신·증빙 파일을 백업한 뒤 같은 원본을 다시 조회했다. 충돌 0건, 수정 대상 1건을 확인해 해당 SE 매도만 `EXPIRED → FILLED`로 반영했다. 주문 건수는 그대로이며 SE 관리 잔량 0·증권사 잔고 차이 0, 미국주식 종료 거래 2건의 성과 집계 복구를 확인했다. 수정 전후는 로컬 증빙 감사 기록에 보존했다. 신규 매도/취소 주문은 보내지 않았다.
